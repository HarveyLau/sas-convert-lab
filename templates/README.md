# Target Templates

These templates guide `sas-designer` and `sas-converter` for each target profile.

- `python-pandas/mapping.yaml`
- `python-bigquery/mapping.yaml`

Each mapping file describes:

- SAS pattern to target mapping
- preferred library or SQL style
- caveats and review hints
