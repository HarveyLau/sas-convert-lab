# 目标模板（Target Templates）

这些模板为 `sas-designer` 与 `sas-converter` 提供不同目标栈的映射约束。

- `python-pandas/mapping.yaml`
- `python-bigquery/mapping.yaml`

每个映射文件应描述：

- SAS 模式到目标实现模式的映射
- 推荐库与 SQL 风格
- 风险提示与评审注意事项
