# Sample Run

Sample artifacts for a single migration run (`run-001`).

Path:

- `examples/sample-run/artifacts/run-001/`

This sample demonstrates:

- intent capture (`vision_brief.json`)
- stage decisions (`decision_log.json`)
- unresolved items (`open_questions.json`)
- phase outputs from discovery to validation
